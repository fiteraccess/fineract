Tests must be linear, local, and concrete: keep setup and assertions adjacent and explicit, isolate state per test,
prefer real domain objects and exact argument matchers, and forbid helpers, shared fixtures, broad matchers, or mocks
that obscure actual values or behavior.
